/**
 * Aumage — Frontend Configuration
 */
const CONFIG = {
  // API_BASE_URL: 'https://aumage-pipeline.rk-pc.workers.dev', // Production
  API_BASE_URL: 'http://localhost:8787', // Local Development (default for wrangler dev)
};

window.CONFIG = CONFIG;
